(function(){
  const API='https://moe-ai-production.up.railway.app';
  const token=()=>sessionStorage.getItem('moeAgentSession')||'';
  const esc=v=>String(v??'—').replaceAll('&','&amp;').replaceAll('<','&lt;').replaceAll('>','&gt;');
  const pct=v=>v==null?'—':`${Number(v)>0?'+':''}${Number(v).toFixed(2)}%`;
  const usd=v=>v==null?'—':Number(v).toLocaleString(undefined,{style:'currency',currency:'USD',maximumFractionDigits:Number(v)>=1000?0:2});
  const delta=v=>v==null?'—':`${Number(v)>0?'+':''}${Number(v).toFixed(1)}`;
  const when=v=>{const d=new Date(v);return Number.isNaN(d.getTime())?'—':d.toLocaleString()};
  async function json(path,headers={}){const r=await fetch(API+path,{cache:'no-store',headers});const j=await r.json().catch(()=>({}));if(!r.ok)throw new Error(j.detail||j.error||`HTTP ${r.status}`);return j}
  async function getFeed(){const t=token();if(t)return json('/v1/radar/weekly?min_score=40&limit=16',{Authorization:'Bearer '+t});try{return await json('/v1/radar/preview?min_score=40&limit=12')}catch(e){const status=await json('/v1/radar/status');return {...status,data:[],preview_endpoint_pending:true}}}
  function row(x){const flags=(x.risk_flags||[]).filter(f=>f!=='discovery_only').slice(0,2),validation=x.security_validated?'SECURITY VERIFIED':(x.holder_intelligence_validated?'HOLDERS VERIFIED':'CORE RESEARCH');return `<article class="radar-card"><div class="radar-token"><strong>${esc(x.symbol)}</strong><span>${esc(x.chain||x.chain_id)} • ${esc(x.stage||'TRACKING')}</span></div><div class="radar-score"><b>${esc(x.radar_score)}</b><span>RADAR SCORE</span></div><div><b>${delta(x.score_delta_7d)}</b><span>7D SCORE</span></div><div><b>${pct(x.price_change_7d_pct)}</b><span>7D PRICE</span></div><div><b>${pct(x.liquidity_change_7d_pct)}</b><span>7D LIQUIDITY</span></div><div><b>${usd(x.liquidity_usd)}</b><span>LIQUIDITY</span></div><div class="radar-state"><b>${esc(x.weekly_status)}</b><span>${esc(x.observations_7d)} observations</span></div><div class="radar-risk"><b>${validation}</b><span>${flags.length?esc(flags.join(' • ')):'No hard risk flag in current core scan'}</span></div></article>`}
  function shell(){if(document.getElementById('weeklyRadarPanel'))return;const agent=document.querySelector('.agent-shell');if(!agent)return;const panel=document.createElement('section');panel.id='weeklyRadarPanel';panel.className='radar-shell';panel.innerHTML=`<div class="radar-top"><div><div class="section-label">MOER TOKEN RADAR • LIVE WEEKLY DISCOVERY</div><h2>7-DAY TOKEN WATCH.</h2><p>Separate from Moer Scanner. Radar follows emerging Base and Solana tokens across structure, liquidity, market cap, volume and risk. It never feeds Moer Agent in the current build.</p></div><button class="button secondary" id="weeklyRadarRefresh">REFRESH RADAR</button></div><div class="radar-mode">DISCOVERY ONLY • EXECUTION FEED DISABLED</div><div class="radar-live-strip"><div><span>CORE RADAR</span><strong id="radarCore">CHECKING</strong></div><div><span>NETWORKS</span><strong>BASE + SOLANA</strong></div><div><span>CADENCE</span><strong>HOURLY</strong></div><div><span>LAST SCAN</span><strong id="radarUpdated">—</strong></div></div><div id="radarProviderNote" class="radar-provider-note">Checking intelligence providers…</div><div id="weeklyRadarRows"><div class="loading">Connecting to live Token Radar…</div></div>`;agent.parentNode.insertBefore(panel,agent);const style=document.createElement('style');style.textContent=`.radar-shell{margin:28px 0;padding:26px;border:1px solid #28395c;border-radius:22px;background:radial-gradient(circle at 10% 0,rgba(0,82,255,.12),transparent 30%),radial-gradient(circle at 95% 0,rgba(126,54,210,.12),transparent 30%),#080d16;overflow:hidden}.radar-top{display:flex;justify-content:space-between;gap:20px;align-items:flex-start}.radar-top h2{margin:6px 0 8px;font-size:32px}.radar-top p{margin:0;max-width:790px;color:#8997b4;line-height:1.65}.radar-mode{margin-top:16px;padding:9px 11px;border:1px solid #2b4265;background:#081526;color:#76bfff;font-size:9px;font-weight:1000;letter-spacing:.08em}.radar-live-strip{display:grid;grid-template-columns:repeat(4,1fr);gap:10px;margin:10px 0}.radar-live-strip>div{padding:13px;border:1px solid #26344c;border-radius:12px;background:#090f1a}.radar-live-strip span{display:block;color:#697996;font-size:8px;font-weight:1000;margin-bottom:6px}.radar-live-strip strong{font-size:13px}.radar-provider-note{margin-bottom:14px;padding:10px 12px;border-left:3px solid #4f78ff;background:#0a1220;color:#91a0bc;font-size:11px;line-height:1.5}.radar-card{display:grid;grid-template-columns:1.25fr .7fr .7fr .75fr .8fr .9fr 1fr 1.35fr;gap:10px;align-items:center;padding:15px 13px;border-top:1px solid #1f2b42;background:linear-gradient(90deg,rgba(0,82,255,.03),rgba(125,55,205,.025))}.radar-card b,.radar-card strong{display:block;color:#eef4ff}.radar-card span{display:block;margin-top:4px;color:#71819e;font-size:8px;line-height:1.35}.radar-token strong{font-size:15px}.radar-score b{font-size:20px;color:#77a0ff}.radar-state b{color:#a7c2ff}.radar-risk b{font-size:9px;color:#9ee9c1}.radar-empty{padding:28px;border:1px dashed #33435d;border-radius:14px;color:#8795ae;text-align:center;line-height:1.6}.radar-live{color:#68f49b!important}.radar-warn{color:#ffd96b!important}@media(max-width:1050px){.radar-card{grid-template-columns:repeat(4,1fr)}.radar-live-strip{grid-template-columns:1fr 1fr}}@media(max-width:650px){.radar-top{flex-direction:column}.radar-card{grid-template-columns:1fr 1fr}.radar-live-strip{grid-template-columns:1fr 1fr}.radar-shell{padding:18px}}`;document.head.appendChild(style);document.getElementById('weeklyRadarRefresh').onclick=load}
  async function load(){const area=document.getElementById('weeklyRadarRows');if(!area)return;area.innerHTML='<div class="loading">Refreshing live Radar…</div>';try{const x=await getFeed(),data=x.data||[],providers=x.providers||{};radarCore.textContent=x.core_radar_live===false?'NO DATA':'LIVE';radarCore.className=x.core_radar_live===false?'radar-warn':'radar-live';radarUpdated.textContent=when(x.latest_update||data[0]?.latest_observed);const on=Object.entries(providers).filter(([,v])=>v).map(([k])=>k.toUpperCase()),off=Object.entries(providers).filter(([,v])=>!v).map(([k])=>k.toUpperCase());radarProviderNote.textContent=off.length?`Core market Radar is live. Advanced intelligence pending credentials: ${off.join(', ')}.${on.length?' Active: '+on.join(', ')+'.':''}`:'Core Radar and advanced intelligence providers are active.';if(data.length)area.innerHTML=data.map(row).join('');else area.innerHTML=`<div class="radar-empty"><strong>CORE TOKEN RADAR IS LIVE</strong><br>${x.preview_endpoint_pending?'Live status is available, but candidate rows are still waiting for the new sanitized preview endpoint to finish rolling out. No fake DEMO rows are displayed.':'No candidates currently meet the selected minimum score.'}</div>`}catch(e){area.innerHTML=`<div class="radar-empty">TOKEN RADAR STATUS UNAVAILABLE — ${esc(e.message)}</div>`}}
  document.addEventListener('DOMContentLoaded',()=>{if(!location.pathname.startsWith('/live/'))return;shell();setTimeout(load,500)});
})();